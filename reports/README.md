# Reports Scaffold

`clean-install` does not ship generated reports.

This directory is reserved for runtime-created summaries, rollups, and exported artifacts.
Canonical docs belong under `docs/`, not under `reports/`.
