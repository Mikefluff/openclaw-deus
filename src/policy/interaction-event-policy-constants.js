"use strict";

const INTERACTION_KIND_ALIASES = Object.freeze({
  approval: "approval",
  confirmation: "approval",
  consent: "approval",
  constraint: "constraint",
  correction: "correction",
  directive: "instruction",
  dislike: "preference",
  incidental: "incidental",
  instruction: "instruction",
  note: "other",
  other: "other",
  personal: "personal_context",
  personal_context: "personal_context",
  preference: "preference",
  project: "project_context",
  project_context: "project_context",
  question: "question",
  request: "instruction",
  small_talk: "incidental",
  topic: "incidental",
});

const INTERACTION_SIGNAL_KEYWORDS = Object.freeze({
  preference: Object.freeze([
    "не нравится",
    "не люблю",
    "предпочитаю",
    "люблю",
    "избегай",
    "всегда",
  ]),
  constraint: Object.freeze([
    "никогда",
    "не делай",
    "не используй",
    "обязательно",
    "нельзя",
    "запрещаю",
    "критично",
  ]),
  instruction: Object.freeze([
    "сделай",
    "нужно",
    "надо",
    "прошу",
    "хочу",
    "давай",
    "важно",
  ]),
  identity: Object.freeze([
    "обо мне",
    "для меня",
    "мой",
    "моя",
    "мне важно",
    "для дэна",
  ]),
  project: Object.freeze([
    "проект",
    "репозиторий",
    "roadmap",
    "deus",
    "openclaw",
    "workspace",
  ]),
});

const INTERACTION_EVENT_POLICY = Object.freeze({
  version: 1,
  defaultActor: "human",
  defaultSource: "chat",
  kinds: Object.freeze({
    approval: "approval",
    constraint: "constraint",
    correction: "correction",
    incidental: "incidental",
    instruction: "instruction",
    other: "other",
    personal_context: "personal_context",
    preference: "preference",
    project_context: "project_context",
    question: "question",
  }),
  salienceBands: Object.freeze({
    ignore: Object.freeze({
      min: 0,
      max: 0.34,
      captureDecision: "ignore",
    }),
    log_only: Object.freeze({
      min: 0.35,
      max: 0.64,
      captureDecision: "log_only",
    }),
    reflectable: Object.freeze({
      min: 0.65,
      max: 0.84,
      captureDecision: "log_and_memory_candidate",
    }),
    review_signal: Object.freeze({
      min: 0.85,
      max: 1,
      captureDecision: "log_memory_and_review_candidate",
    }),
  }),
  thresholds: Object.freeze({
    log: 0.35,
    memoryCandidate: 0.65,
    reviewSignal: 0.85,
  }),
  kindWeights: Object.freeze({
    approval: 0.5,
    constraint: 0.7,
    correction: 0.45,
    incidental: 0.08,
    instruction: 0.62,
    other: 0.18,
    personal_context: 0.35,
    preference: 0.55,
    project_context: 0.25,
    question: 0.12,
  }),
  signalWeights: Object.freeze({
    explicitPreference: 0.15,
    explicitConstraint: 0.18,
    explicitInstruction: 0.12,
    continuityRelevant: 0.1,
    identityRelevant: 0.12,
    projectRelevant: 0.08,
    approvalChange: 0.08,
    correction: 0.08,
    recurringReference: 0.1,
  }),
});

module.exports = {
  INTERACTION_EVENT_POLICY,
  INTERACTION_KIND_ALIASES,
  INTERACTION_SIGNAL_KEYWORDS,
};
