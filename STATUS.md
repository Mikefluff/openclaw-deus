# STATUS.md

> Canonical live status file for DEUS
> Update this file when entering or leaving a meaningful work block.

## Current state
- mode: idle
- active_project: deus
- current_focus: maintain a public-safe clean-install starter state
- status: idle
- follow_through_required: no

## Open work block
- goal: keep the clean-install bootstrap surfaces valid until a local operator personalizes private runtime state
- next_step: personalize USER.md locally, then resume project-specific work in private runtime artifacts
- unblock_condition: a local operator defines project context, credentials, and runtime-specific goals

## Last completed block
- completed: initialized a generic clean-install starter state for public-safe bootstrap and verification
- completed_at: set locally when a real runtime block is completed

## Notes
- `status=active` means there is still an unfinished block worth continuing when automation checks the file.
- Set `status=waiting` when blocked on human input or an external dependency.
- Set `status=idle` when no active follow-through is needed.
