# Runtime Snapshot Scaffold

`clean-install` does not ship exported runtime snapshots.

This directory exists only as a placeholder for optional local exports.
Snapshot material should be generated locally from a real runtime, never bundled into the public baseline.
