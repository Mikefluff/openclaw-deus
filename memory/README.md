# Memory Scaffold

`clean-install` does not ship dated memory history.

This directory is intentionally scaffold-only.
Runtime memory files such as `memory/YYYY-MM-DD.md` should be created locally by DEUS during real operation.

Rule:
- keep reusable docs and code generic;
- keep session continuity in local runtime artifacts, not in the public branch.
