# Logs Scaffold

`clean-install` does not ship operational log history.

This directory is created in the public baseline only as a runtime scaffold.
Daily log streams such as `logs/YYYY-MM-DD.jsonl` should be created locally when the agent actually runs.
